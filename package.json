{
  "name": "css-animations",
  "version": "1.0.0",
  "description": "CSS translations are the foundation of CSS animations.",
  "main": "transforms.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  }
}
